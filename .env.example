# A qué API reenvía nginx /api/. Host sin esquema ni barra final.

# Contenedor api-fiestas en el mismo VPS (su docker-compose, red proxy_network):
API_UPSTREAM=api-fiestas:8787
API_ESQUEMA=http

# O el Worker de Cloudflare:
# API_UPSTREAM=api-fiestas.TU-SUBDOMINIO.workers.dev
# API_ESQUEMA=https
